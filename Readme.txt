I have used system available maven to build using the command

mvn clean package -DskipTests // This cleans the targets/ directory and packages and build a new JAR file
                              // -DskipTests skips unti tests, since it takes a longer time. 
